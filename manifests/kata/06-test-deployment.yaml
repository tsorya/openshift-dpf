# Kata smoke-test Deployment. Injector adds one VF from the shared pool
# (dpf-ovn-kubernetes / openshift.io/bf3_vfs), same as regular DPU pods.
# Scale with KATA_TEST_REPLICAS (default 1). Apply via make deploy-kata-test.
#
# Prerequisites: KATA_ENABLED=true, make enable-ovn-injector, make enable-kata.
# maxSurge: 0 avoids oversubscribing VFs during rolling updates.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: kata-dpu-test
  labels:
    app: kata-dpu-test
spec:
  replicas: <KATA_TEST_REPLICAS>
  selector:
    matchLabels:
      app: kata-dpu-test
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
  template:
    metadata:
      labels:
        app: kata-dpu-test
    spec:
      runtimeClassName: <KATA_RUNTIME_CLASS>
      nodeSelector:
        node-role.kubernetes.io/<WORKER_ROLE>: ""
      topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
        labelSelector:
          matchLabels:
            app: kata-dpu-test
      containers:
      - name: test
        image: quay.io/wabouham/ecosys-nvidia/nicolaka-netshoot:latest
        command: ["sleep", "infinity"]
