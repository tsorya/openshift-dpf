# Deprecated: recursive log deletion every five minutes erases Argus evidence
# during demos. Argus native rotation is configured in 03-configuration.yaml
# (log_threshold_size / log_max_files_count). enable-argus.sh no longer applies
# this manifest. Use scripts/argus-gtc-demo.sh deploy to remove any legacy cleaner.
